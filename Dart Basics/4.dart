/*Write a program in Dart that finds simple interest. 
Formula= (p * t * r) / 100*/

void main() {
  var p = 5;
  int t = 3;
  double r = 7;

  double formula = (p * t * r) / 100;
  print(formula);
}
